language: node_js
node_js:
  - lts/argon
  - lts/boron
  - stable
cache:
  directories:
    - node_modules
before_install:
  - if [[ $(npm --version) == 2* ]]; then npm install -g npm@latest-3; fi
after_success:
  - npm i -g coveralls
  - cat coverage/remapped/lcov.info | coveralls
